<!DOCTYPE html>
<html>
  <head>
    <title>Baldur3</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <script src="https://kit.fontawesome.com/7abb47e8ad.js" crossorigin="anonymous"></script>
    <%= stylesheet_link_tag "application", "data-turbo-track": "load" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="my-custom-bg-success ">
  
  <% if flash[:notice].present? %>
  <div class="notice bg-info"><%= flash[:notice] %></div>
  <% end %>



    <div class="container-flex">
      <div class="row">
          <div class="col-1">
          <%= render 'shared/menu' %>  
          </div>
          <div class="col-11">
            <%= yield %>
            </div>
      </div>
    </div>
    
  </body>
</html>
